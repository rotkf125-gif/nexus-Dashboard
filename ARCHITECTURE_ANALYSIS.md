# NEXUS Dashboard - 아키텍처 분석 및 피드백 보고서

**프로젝트**: NEXUS Portfolio Dashboard v1.6.0  
**분석 일자**: 2026-01-17  
**분석 범위**: 전체 프로젝트 구조, 코드 품질, 의존성, 아키텍처 패턴

---

## 📊 프로젝트 개요

### 기술 스택
- **프레임워크**: Next.js 14.2.0 (App Router)
- **언어**: TypeScript 5.x (Strict Mode)
- **UI**: React 18.2.0 + Tailwind CSS 3.4.1
- **상태 관리**: React Context API + Custom Hooks
- **데이터 저장**: Supabase + LocalStorage (Hybrid)
- **차트**: Chart.js 4.4.1 + Recharts 3.6.0
- **테스트**: Vitest 4.0.16 + Testing Library
- **AI 통합**: Google Generative AI 0.24.1

### 프로젝트 규모
- **컴포넌트**: 38개
- **API 엔드포인트**: 8개
- **커스텀 훅**: 7개
- **타입 정의**: 250+ 라인
- **총 코드 라인**: ~15,000+ (추정)

---

## 🏗️ 아키텍처 구조

### 1. 디렉토리 구조

```
nexus-next/
├── app/                    # Next.js App Router
│   ├── api/               # API Routes (8개)
│   │   ├── benchmark/     # 벤치마크 데이터
│   │   ├── freedom/       # Freedom 분석
│   │   ├── info/          # 티커 정보
│   │   ├── market/        # 시장 데이터
│   │   ├── price/         # 가격 데이터
│   │   └── widget/        # 위젯 데이터
│   ├── layout.tsx         # 루트 레이아웃
│   └── page.tsx           # 메인 대시보드
│
├── components/            # React 컴포넌트 (38개)
│   ├── income/           # Income 관련 서브 컴포넌트
│   ├── *Modal.tsx        # 모달 컴포넌트들
│   ├── *Table.tsx        # 테이블 컴포넌트들
│   └── *.tsx             # 기타 UI 컴포넌트들
│
├── lib/                   # 핵심 로직 & 유틸리티
│   ├── hooks/            # 커스텀 훅 (7개)
│   ├── __tests__/        # 단위 테스트
│   ├── config.ts         # 설정 상수
│   ├── context.tsx       # 전역 상태 관리 (777줄)
│   ├── storage.ts        # 데이터 영속성
│   ├── supabase.ts       # Supabase 클라이언트
│   ├── types.ts          # TypeScript 타입 정의 (251줄)
│   └── utils.ts          # 유틸리티 함수
│
├── public/               # 정적 파일
│   ├── icons/           # PWA 아이콘
│   ├── manifest.json    # PWA 매니페스트
│   └── sw.js            # Service Worker
│
├── styles/              # 글로벌 스타일
│   └── globals.css      # Tailwind + 커스텀 CSS
│
└── test/                # 테스트 설정 & 모킹
    ├── mocks/           # API 모킹
    ├── server/          # 테스트 서버
    └── utils/           # 테스트 유틸리티
```

---

## 🔄 데이터 흐름 및 의존성

### 1. 중앙 집중식 상태 관리 (Context API)

```
┌─────────────────────────────────────────────────────────┐
│                    NexusProvider                         │
│                   (lib/context.tsx)                      │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │           NexusState (전역 상태)                │    │
│  │  - assets: Asset[]                              │    │
│  │  - dividends: Dividend[]                        │    │
│  │  - tradeLogs: TradeLog[]                        │    │
│  │  - tradeSums: TradeSums                         │    │
│  │  - market: MarketData                           │    │
│  │  - exchangeRate: number                         │    │
│  │  - theme, compactMode, etc.                     │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │        60+ Actions & Handlers                   │    │
│  │  - CRUD: addAsset, updateAsset, removeAsset     │    │
│  │  - Modal: openAddAssetModal, closeAssetModal   │    │
│  │  - Sync: refreshPrices, syncFromSheet          │    │
│  │  - Undo/Redo: undo(), redo()                   │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          ↓
        ┌─────────────────┴─────────────────┐
        ↓                                    ↓
   useNexus()                          useNexus()
   (38개 컴포넌트)                     (커스텀 훅)
```

**장점:**
- ✅ 단일 진실 공급원 (Single Source of Truth)
- ✅ Props Drilling 방지
- ✅ 일관된 상태 업데이트 패턴

**단점:**
- ⚠️ Context 파일이 777줄로 과도하게 비대함
- ⚠️ 모든 컴포넌트가 전체 Context를 구독 (불필요한 리렌더링 가능성)
- ⚠️ 60+ 액션이 하나의 Context에 집중

### 2. 데이터 영속성 계층 (Hybrid Storage)

```
┌──────────────────────────────────────────────────────┐
│                  Storage Layer                        │
│                  (lib/storage.ts)                     │
│                                                       │
│  ┌────────────────┐      ┌────────────────┐         │
│  │  LocalStorage  │ ←──→ │   Supabase     │         │
│  │  (즉시 저장)    │      │  (클라우드 동기화)│         │
│  └────────────────┘      └────────────────┘         │
│         ↑                        ↑                   │
│         │                        │                   │
│    saveState()            saveStateToSupabase()      │
│    loadState()            loadStateFromSupabase()    │
│                                                       │
│  ┌────────────────────────────────────────┐         │
│  │  Snapshot (30분 간격)                   │         │
│  │  - 포트폴리오 스냅샷 저장                │         │
│  │  - 히스토리 추적용                       │         │
│  └────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────┘
```

**장점:**
- ✅ 오프라인 우선 (LocalStorage)
- ✅ 클라우드 동기화 (Supabase)
- ✅ Debounce로 과도한 저장 방지 (1초)

**단점:**
- ⚠️ 동기화 충돌 해결 로직 부재
- ⚠️ LocalStorage 용량 제한 고려 필요 (5-10MB)

### 3. API 계층 (Next.js API Routes)

```
┌─────────────────────────────────────────────────────┐
│                  API Routes                          │
│                  (app/api/)                          │
│                                                      │
│  /api/market        → 시장 데이터 (NASDAQ, S&P500)  │
│  /api/price/[ticker] → 개별 주식 가격               │
│  /api/info/[ticker]  → 티커 정보 (섹터, 이름)       │
│  /api/benchmark     → 벤치마크 비교                 │
│  /api/widget        → 위젯 데이터                   │
│  /api/freedom       → Freedom 분석                  │
│                                                      │
│  ┌────────────────────────────────────────┐         │
│  │  External Data Sources                  │         │
│  │  - Yahoo Finance (추정)                 │         │
│  │  - Market Data APIs                     │         │
│  └────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────┘
```

**장점:**
- ✅ 서버사이드 API 호출 (CORS 우회)
- ✅ 캐싱 가능
- ✅ 테스트 커버리지 존재 (market, price)

**단점:**
- ⚠️ 에러 핸들링 일관성 부족
- ⚠️ Rate Limiting 없음
- ⚠️ API 응답 캐싱 전략 부재

---

## 🧩 컴포넌트 의존성 분석

### 1. 컴포넌트 계층 구조

```
app/page.tsx (루트)
├── Header
│   ├── PortfolioStats (계산)
│   └── ActionButtons
│
├── PortfolioHealthAlert
│   └── VIX 기반 경고
│
├── StrategyBar
│   └── 전략 입력
│
├── Tabs (5개)
│   ├── Stellar (메인)
│   │   ├── AssetTable / PortfolioHeatmap
│   │   ├── Analytics
│   │   └── TradeJournal
│   │
│   ├── Income
│   │   ├── IncomeStream
│   │   │   ├── IncomeCard (서브)
│   │   │   ├── WeeklySummary (서브)
│   │   │   └── RecentLogs (서브)
│   │   ├── DividendAnalytics
│   │   ├── DividendCalendar
│   │   └── DividendOptimizer
│   │
│   ├── Analytics
│   │   ├── MonthlyReport
│   │   ├── AssetTurnover
│   │   └── RebalanceSuggestion
│   │
│   ├── Performance
│   │   ├── PerformanceArena
│   │   ├── HistoricPerformance
│   │   └── StressTest
│   │
│   └── Simulation
│       ├── SimulationHub
│       │   ├── WhatIfSimulator
│       │   └── RebalanceSimulator
│       └── DPSTrend
│
└── Modals (6개)
    ├── AssetModal
    ├── DividendModal
    ├── TradeModal (사용 안 함)
    ├── SettingsModal
    ├── AuthModal
    └── FreedomModal
```

### 2. 컴포넌트 의존성 매트릭스

| 컴포넌트 | useNexus | 외부 API | 차트 라이브러리 | 복잡도 |
|---------|----------|---------|---------------|--------|
| AssetTable | ✅ | ❌ | ❌ | 높음 |
| IncomeStream | ✅ | ❌ | ❌ | 매우 높음 |
| Analytics | ✅ | ❌ | ✅ Chart.js | 높음 |
| MonthlyReport | ✅ | ❌ | ❌ | 중간 |
| PerformanceArena | ✅ | ❌ | ✅ Recharts | 높음 |
| Header | ✅ | ✅ (가격) | ❌ | 높음 |
| TradeJournal | ✅ | ❌ | ❌ | 낮음 |

---

## 💡 핵심 기능 분석

### 1. 포트폴리오 관리
- **자산 CRUD**: 추가, 수정, 삭제, 조회
- **실시간 가격 업데이트**: 1분 간격 자동 갱신
- **환율 관리**: 매수 시 환율 vs 현재 환율 분리
- **자산 유형**: CORE, INCOME, GROWTH, VALUE, SPECULATIVE

### 2. 배당 추적
- **배당 기록**: 날짜, 티커, 수량, DPS
- **세후 배당**: 0.85 비율 자동 적용
- **월별 집계**: 배당 캘린더, 분석
- **예측**: 평균 DPS 기반 예측

### 3. 거래 일지 (최근 단순화)
- **기존**: BUY/SELL, QTY, PRICE, FEE → FIFO 계산
- **현재**: 티커 + 실현금액(+/-) 수기 입력
- **실현 손익**: 티커별 누적 관리

### 4. 분석 & 리포트
- **월간 보고서**: 포트폴리오 요약, 상위 종목, 유형별 분포
- **성능 분석**: 벤치마크 비교, 히스토리 추적
- **리스크 분석**: VIX 기반 경고, 스트레스 테스트
- **시뮬레이션**: What-If, 리밸런싱

### 5. Undo/Redo
- **히스토리**: 최대 10개 상태 저장
- **지원 액션**: 자산 추가/수정/삭제, 거래 기록
- **UI 표시**: UndoRedoIndicator 컴포넌트

---

## ⚠️ 발견된 문제점

### 🔴 Critical Issues

#### 1. Context 비대화 (777줄)
**문제:**
```typescript
// lib/context.tsx - 777줄
// 60+ 액션, 모달 상태, Undo/Redo, 데이터 로직 모두 포함
```

**영향:**
- 유지보수 어려움
- 테스트 복잡도 증가
- 불필요한 리렌더링 가능성

**해결 방안:**
```typescript
// 분리 제안
lib/
├── contexts/
│   ├── PortfolioContext.tsx  (자산 관리)
│   ├── DividendContext.tsx   (배당 관리)
│   ├── TradeContext.tsx      (거래 관리)
│   ├── UIContext.tsx         (모달, 테마)
│   └── MarketContext.tsx     (시장 데이터)
```

#### 2. TradeModal 미사용 코드
**문제:**
- `components/TradeModal.tsx` 존재하지만 사용 안 함
- Context에 TradeModal 관련 상태/액션 여전히 존재

**해결 방안:**
```bash
# 제거 필요
rm components/TradeModal.tsx
# Context에서 관련 코드 제거
```

#### 3. 타입 안전성 부족
**문제:**
```typescript
// 여러 곳에서 any 또는 타입 단언 사용
const data = await res.json() as any;
```

**해결 방안:**
- API 응답 타입 명시적 정의
- Zod 또는 io-ts로 런타임 검증

### 🟡 Medium Issues

#### 4. 컴포넌트 크기
**문제:**
- `IncomeStream.tsx`: 462줄
- `Header.tsx`: 482줄
- `MonthlyReport.tsx`: 443줄

**해결 방안:**
- 서브 컴포넌트로 분리
- 커스텀 훅으로 로직 추출

#### 5. 중복 계산 로직
**문제:**
```typescript
// 여러 컴포넌트에서 동일한 계산 반복
const totalValue = assets.reduce((sum, a) => sum + a.qty * a.price, 0);
const totalCost = assets.reduce((sum, a) => sum + a.qty * a.avg, 0);
```

**해결 방안:**
```typescript
// lib/hooks/usePortfolioStats.ts
export function usePortfolioStats() {
  const { state } = useNexus();
  return useMemo(() => calculateStats(state.assets), [state.assets]);
}
```

#### 6. 에러 처리 일관성 부족
**문제:**
```typescript
// 일부는 try-catch, 일부는 .catch(), 일부는 무시
try { ... } catch { console.error(...) }
fetch().catch(() => {})
```

**해결 방안:**
- 전역 에러 바운더리
- 일관된 에러 핸들링 패턴
- 사용자 친화적 에러 메시지

### 🟢 Minor Issues

#### 7. 하드코딩된 값
```typescript
const AFTER_TAX_RATE = 0.85; // 여러 파일에 중복
```

**해결 방안:**
- `lib/config.ts`로 이동

#### 8. 테스트 커버리지 부족
- API 라우트 일부만 테스트 (market, price)
- 컴포넌트 테스트 없음
- 통합 테스트 없음

#### 9. 접근성 (a11y)
- 키보드 네비게이션 부족
- ARIA 레이블 부재
- 스크린 리더 지원 미흡

---

## ✅ 잘된 점

### 1. TypeScript 활용
- ✅ Strict 모드 활성화
- ✅ 250+ 라인의 타입 정의
- ✅ 인터페이스 기반 설계

### 2. 모듈화
- ✅ 명확한 디렉토리 구조
- ✅ 컴포넌트 분리
- ✅ 유틸리티 함수 분리

### 3. 성능 최적화
- ✅ `useMemo`, `useCallback` 적극 활용
- ✅ Debounce로 저장 최적화 (1초)
- ✅ 조건부 렌더링

### 4. PWA 지원
- ✅ Service Worker
- ✅ Manifest.json
- ✅ 오프라인 지원 (LocalStorage)

### 5. 데이터 영속성
- ✅ Hybrid Storage (LocalStorage + Supabase)
- ✅ 자동 저장
- ✅ 스냅샷 기능 (30분 간격)

### 6. Undo/Redo
- ✅ 10단계 히스토리
- ✅ 주요 액션 지원
- ✅ UI 표시

---

## 📈 개선 제안 (우선순위별)

### 🔥 High Priority

#### 1. Context 분리
```typescript
// Before: 1개의 거대한 Context (777줄)
// After: 5개의 작은 Context

// lib/contexts/PortfolioContext.tsx
export const PortfolioProvider = ({ children }) => {
  // 자산 관리 로직만
};

// lib/contexts/index.tsx
export const AppProviders = ({ children }) => (
  <PortfolioProvider>
    <DividendProvider>
      <TradeProvider>
        <UIProvider>
          <MarketProvider>
            {children}
          </MarketProvider>
        </UIProvider>
      </TradeProvider>
    </DividendProvider>
  </PortfolioProvider>
);
```

#### 2. 미사용 코드 제거
```bash
# TradeModal 완전 제거
rm components/TradeModal.tsx
# Context에서 관련 코드 제거
# - tradeModalOpen, editingTrade
# - openAddTradeModal, closeTradeModal, saveTradeFromModal
```

#### 3. 에러 처리 표준화
```typescript
// lib/errors.ts
export class APIError extends Error {
  constructor(public status: number, message: string) {
    super(message);
  }
}

// lib/hooks/useAPICall.ts
export function useAPICall<T>() {
  const [data, setData] = useState<T | null>(null);
  const [error, setError] = useState<Error | null>(null);
  const [loading, setLoading] = useState(false);

  const execute = async (fetcher: () => Promise<T>) => {
    setLoading(true);
    try {
      const result = await fetcher();
      setData(result);
      return result;
    } catch (err) {
      setError(err as Error);
      throw err;
    } finally {
      setLoading(false);
    }
  };

  return { data, error, loading, execute };
}
```

### 🔶 Medium Priority

#### 4. 컴포넌트 분해
```typescript
// components/IncomeStream.tsx (462줄)
// → 분리

components/income/
├── IncomeStream.tsx (메인, 100줄)
├── IncomeCard.tsx (이미 존재)
├── IncomeStats.tsx (통계)
├── IncomeChart.tsx (차트)
└── IncomeDividendList.tsx (배당 목록)
```

#### 5. 커스텀 훅 확장
```typescript
// lib/hooks/usePortfolioStats.ts
export function usePortfolioStats() {
  const { state } = useNexus();
  
  return useMemo(() => ({
    totalValue: calculateTotalValue(state.assets),
    totalCost: calculateTotalCost(state.assets),
    profit: calculateProfit(state.assets),
    returnPct: calculateReturnPct(state.assets),
    // ... 기타 통계
  }), [state.assets]);
}

// 사용
const stats = usePortfolioStats();
```

#### 6. API 응답 타입 정의
```typescript
// lib/types.ts
export interface PriceAPIResponse {
  ticker: string;
  price: number;
  change: number;
  changePercent: number;
  timestamp: number;
}

// app/api/price/[ticker]/route.ts
export async function GET(req: Request): Promise<Response> {
  const data: PriceAPIResponse = await fetchPrice(ticker);
  return Response.json(data);
}
```

### 🔷 Low Priority

#### 7. 테스트 추가
```typescript
// components/__tests__/TradeJournal.test.tsx
describe('TradeJournal', () => {
  it('should display trades', () => {
    // ...
  });
  
  it('should add new trade', () => {
    // ...
  });
});
```

#### 8. 접근성 개선
```typescript
// 키보드 네비게이션
<button
  onClick={handleClick}
  onKeyDown={(e) => e.key === 'Enter' && handleClick()}
  aria-label="Add new asset"
>
```

#### 9. 문서화
```typescript
/**
 * 포트폴리오의 총 평가금액을 계산합니다.
 * @param assets - 자산 배열
 * @returns 총 평가금액 (USD)
 */
export function calculateTotalValue(assets: Asset[]): number {
  return assets.reduce((sum, a) => sum + a.qty * a.price, 0);
}
```

---

## 🎯 리팩토링 로드맵

### Phase 1: 긴급 (1-2주)
1. ✅ TradeModal 제거 (완료)
2. ⏳ Context 분리 (PortfolioContext, UIContext)
3. ⏳ 에러 처리 표준화

### Phase 2: 중요 (2-4주)
4. ⏳ 큰 컴포넌트 분해 (IncomeStream, Header)
5. ⏳ 커스텀 훅 확장 (usePortfolioStats, useTradeStats)
6. ⏳ API 타입 정의

### Phase 3: 개선 (4-8주)
7. ⏳ 테스트 커버리지 80% 달성
8. ⏳ 접근성 개선
9. ⏳ 문서화 (JSDoc, README)

---

## 📊 성능 분석

### 번들 크기 (추정)
- **전체**: ~500KB (gzip 전)
- **Chart.js**: ~200KB
- **React + Next.js**: ~150KB
- **기타 라이브러리**: ~100KB
- **앱 코드**: ~50KB

### 최적화 기회
1. **Code Splitting**: 탭별 동적 import
2. **Tree Shaking**: 사용하지 않는 Chart.js 모듈 제거
3. **이미지 최적화**: Next.js Image 컴포넌트 사용
4. **폰트 최적화**: Font Display Swap

---

## 🔒 보안 고려사항

### 현재 상태
- ✅ Supabase RLS (Row Level Security) 사용 추정
- ✅ API Routes로 외부 API 키 숨김
- ⚠️ XSS 방어: React의 기본 방어에 의존
- ⚠️ CSRF 방어: 없음 (필요 시 추가)

### 개선 제안
1. **환경 변수**: `.env.local`에 API 키 저장
2. **Rate Limiting**: API 엔드포인트에 추가
3. **Input Validation**: Zod로 입력 검증
4. **Content Security Policy**: Next.js 헤더 설정

---

## 📝 종합 평가

### 점수 (10점 만점)

| 항목 | 점수 | 평가 |
|-----|------|------|
| **아키텍처 설계** | 7/10 | Context 비대화 문제, 전반적으로 양호 |
| **코드 품질** | 7.5/10 | TypeScript 활용 우수, 일부 중복 |
| **성능** | 8/10 | useMemo/useCallback 활용, 최적화 여지 |
| **유지보수성** | 6.5/10 | 큰 파일들, 문서화 부족 |
| **테스트** | 4/10 | API 일부만, 컴포넌트 테스트 없음 |
| **보안** | 7/10 | 기본적 보안, 개선 여지 |
| **접근성** | 5/10 | 키보드/스크린리더 지원 부족 |
| **문서화** | 5/10 | README 존재, JSDoc 부족 |

**총점**: **6.9/10** (양호)

### 강점
1. ✅ TypeScript Strict 모드
2. ✅ 명확한 디렉토리 구조
3. ✅ PWA 지원
4. ✅ Hybrid Storage (오프라인 + 클라우드)
5. ✅ Undo/Redo 기능
6. ✅ 성능 최적화 (useMemo/useCallback)

### 약점
1. ⚠️ Context 비대화 (777줄)
2. ⚠️ 테스트 커버리지 부족
3. ⚠️ 큰 컴포넌트들 (400+ 줄)
4. ⚠️ 에러 처리 일관성 부족
5. ⚠️ 접근성 미흡
6. ⚠️ 문서화 부족

---

## 🎓 학습 포인트

### 잘 적용된 패턴
1. **Context API**: 전역 상태 관리
2. **Custom Hooks**: 로직 재사용
3. **Compound Components**: Modal 패턴
4. **Optimistic Updates**: 즉시 UI 업데이트

### 개선이 필요한 패턴
1. **God Object**: Context가 너무 많은 책임
2. **Prop Drilling**: 일부 컴포넌트에서 발생
3. **Magic Numbers**: 하드코딩된 상수들

---

## 🚀 결론

NEXUS Dashboard는 **잘 설계된 포트폴리오 관리 애플리케이션**입니다. TypeScript, Next.js, React의 모범 사례를 대부분 따르고 있으며, PWA 지원과 Hybrid Storage로 사용자 경험을 극대화했습니다.

**주요 개선 과제**는 Context 분리, 테스트 추가, 큰 컴포넌트 분해입니다. 이를 통해 유지보수성과 확장성을 크게 향상시킬 수 있습니다.

**추천 우선순위**:
1. Context 분리 (가장 시급)
2. 미사용 코드 제거
3. 에러 처리 표준화
4. 테스트 추가
5. 문서화

---

**분석자**: AI Assistant  
**분석 도구**: Codebase Analysis, Static Code Review  
**다음 리뷰**: 3개월 후 또는 주요 기능 추가 시
